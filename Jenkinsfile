ipeline {
    agent { docker { image 'httpd' } }
    stages {
        stage('build') {
          steps {
                sh 'echo "testing"'
            }
        }
      }
       stage('Test') {
  	steps {
  		echo 'Testing..'
  		}	
  }
}
